<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:orientation="vertical"
             android:layout_width="match_parent"
             android:layout_height="match_parent">
    <TextView
            android:text="Тут пока ничего нет"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:id="@+id/profile_mytrips_empty"
            android:layout_gravity="center"
            android:textSize="20sp"
            android:visibility="gone"
            android:textColor="@color/textBlack"/>
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/my_trips_swipe_refresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <androidx.recyclerview.widget.RecyclerView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:id="@+id/my_trips_recycler"/>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!--<com.google.android.material.button.MaterialButton-->
    <!--style="@style/TrippleTheme.LaunchButton"-->
    <!--android:layout_width="match_parent"-->
    <!--android:layout_height="64dp"-->
    <!--android:layout_margin="16dp"-->
    <!--app:cornerRadius="25dp"-->
    <!--android:textSize="16sp"-->

    <!--android:text="Добавить свой трип"-->

    <!--app:layout_constraintStart_toStartOf="parent" android:id="@+id/new_trips_button"-->
    <!--android:layout_marginBottom="8dp" app:layout_constraintBottom_toBottomOf="parent"/>-->

</FrameLayout>